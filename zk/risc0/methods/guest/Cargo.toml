[package]
name = "payguard-risc0-guest"
edition = "2021"
version = "0.1.0"
license = "MIT"

[dependencies]
risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"

[workspace]
# Empty workspace table isolates guest compilation from parent workspace
