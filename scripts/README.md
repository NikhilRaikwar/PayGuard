# Scripts

Planned script entrypoints:

- `deploy-testnet.ps1` - build and deploy the gatekeeper contract
- `demo.ps1` - run API and web app for local demo
- `prove.ps1` - call the configured RISC Zero host prover

The app can already be run with `npm run dev:api` and `npm run dev:web`.
